name: kids_portfolio
description: A Flutter web app showcasing the creative work of Joanna Ooms and Eleanor Ooms
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase dependencies
  firebase_core: ^2.24.0
  firebase_storage: ^11.5.0
  cloud_firestore: ^4.13.0
  
  # UI dependencies
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Add assets folder for local images if needed
  # assets:
  #   - assets/images/
